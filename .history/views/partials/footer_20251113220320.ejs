<!-- ========================
     الفوتر (Partial)
======================== -->
<footer class="farm-footer" dir="rtl" aria-labelledby="footer-title">
  <h2 id="footer-title" class="sr-only">تذييل الموقع</h2>

  <div class="footer-top">
    <!-- الشعار والوصف المختصر -->
    <div class="footer-brand">
      <div class="logo-mark" aria-hidden="true">🌿</div>
      <div>
        <strong class="brand-name">مزرعتي</strong>
        <p class="brand-desc">
          منصّة لعرض وطلب المزارع (بيع/إيجار) وربط المزارعين بالمقاولين والخبراء.
        </p>
      </div>
    </div>

    <!-- روابط -->
    <nav class="footer-links" aria-label="روابط الفوتر">
      <!-- تصفح -->
      <div>
        <h3 class="footer-title">تصفح</h3>
        <ul>
          <li><a href="/sale">مزارع للبيع</a></li>
          <li><a href="/rent">مزارع للإيجار</a></li>
          <li><a href="/contractors">المقاولون</a></li>
          <li><a href="/plans">الاشتراكات</a></li>
        </ul>
      </div>

      <!-- عن المنصة (يحتوي كل عناوين صفحة about) -->
      <div>
        <h3 class="footer-title">عن المنصة</h3>
        <ul>
          <li><a href="/about#about">من نحن</a></li>
          <li><a href="/about#terms">سياسة الاستخدام وإخلاء المسؤولية</a></li>
          <li><a href="/about#privacy">سياسة الخصوصية</a></li>
          <li><a href="/about#faq">الأسئلة الشائعة</a></li>
          <li><a href="/about#join">انضم كمعلن</a></li>
          <li><a href="/about#ads-policy">سياسة النشر والإعلانات</a></li>
          <li><a href="/about#report">نموذج الإبلاغ</a></li>
          <li><a href="/about#blog">المدونة</a></li>
        </ul>
      </div>

      <!-- الدعم + تواصل (الدعم ثابت، تواصل ديناميكي من لوحة التحكم) -->
   

<%
  const f = (typeof footer !== 'undefined' && footer) ? footer : {};
  const phoneHref = (f.phone || '').toString().replace(/\s+/g,'');
  const waDigits  = String(f.whatsapp || '').replace(/\D/g,'');
  const hasContact = !!(f.email || f.phone || f.whatsapp || f.address);
%>

<!-- تواصل (عمود مستقل) -->
<% if (hasContact) { %>
<div>
  <h3 class="footer-title">تواصل</h3>
  <ul class="contact">
    <% if (f.email) { %><li><a href="mailto:<%= f.email %>"><%= f.email %></a></li><% } %>
    <% if (f.phone) { %><li><a dir="ltr" href="tel:<%= phoneHref %>"><%= f.phone %></a></li><% } %>
    <% if (f.whatsapp) { %><li><a dir="ltr" href="https://wa.me/<%= waDigits %>" target="_blank" rel="noopener">واتساب</a></li><% } %>
    <% if (f.address) { %><li><span><%= f.address %></span></li><% } %>
  </ul>
</div>
<% } %>

    </nav>
  </div>

  <div class="footer-bottom">
    <!-- سوشيال ديناميكي: تظهر الأيقونة فقط لو الرابط موجود -->
    <div class="social" aria-label="شبكات اجتماعية">
      <% if (f && f.twitter) { %>
        <a class="s-link" href="<%= f.twitter %>" aria-label="تويتر" title="تويتر" rel="noopener" target="_blank">
          <svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">
            <path fill="currentColor" d="M22 5.8c-.7.3-1.5.6-2.3.7.8-.5 1.4-1.3 1.7-2.2-.8.5-1.7.9-2.6 1.1A3.9 3.9 0 0 0 12 8.6c0 .3 0 .5.1.8A11 11 0 0 1 3 5.4a3.9 3.9 0 0 0 1.2 5.2c-.6 0-1.2-.2-1.7-.5v.1c0 1.9 1.3 3.6 3.2 4-.3.1-.7.2-1 .2-.2 0-.5 0-.7-.1.5 1.6 2 2.8 3.8 2.8A7.8 7.8 0 0 1 2 19.5c1.7 1.1 3.8 1.8 6 1.8 7.2 0 11.2-6 11.2-11.2v-.5c.8-.6 1.4-1.3 1.8-2.1z"/>
          </svg>
        </a>
      <% } %>

      <% if (f && f.facebook) { %>
        <a class="s-link" href="<%= f.facebook %>" aria-label="فيسبوك" title="فيسبوك" rel="noopener" target="_blank">
          <svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">
            <path fill="currentColor" d="M13 22v-8h3l1-4h-4V7c0-1.2.3-2 2-2h2V1h-3c-3 0-5 2-5 5v4H6v4h3v8h4z"/>
          </svg>
        </a>
      <% } %>

      <% if (f && f.youtube) { %>
        <a class="s-link" href="<%= f.youtube %>" aria-label="يوتيوب" title="يوتيوب" rel="noopener" target="_blank">
          <svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">
            <path fill="currentColor" d="M23.5 6.2a3 3 0 0 0-2.1-2.1C19.2 3.5 12 3.5 12 3.5s-7.2 0-9.4.6A3 3 0 0 0 .5 6.2 31 31 0 0 0 0 12a31 31 0 0 0 .5 5.8 3 3 0 0 0 2.1 2.1c2.2.6 9.4.6 9.4.6s7.2 0 9.4-.6a3 3 0 0 0 2.1-2.1A31 31 0 0 0 24 12a31 31 0 0 0-.5-5.8zM9.8 15.5V8.6ل6.2 3.5-6.2 3.4z"/>
          </svg>
        </a>
      <% } %>

      <% /* لو أضفت لاحقًا instagram/tiktok في لوحة التحكم، كرّر نفس النمط أعلاه مع أيقونات مناسبة */ %>
    </div>

    <p class="copyright">
      © <span id="year-placeholder"><%= new Date().getFullYear() %></span> فارمرز — جميع الحقوق محفوظة.
    </p>

    <div class="mini-links">
      <a href="/about#privacy">الخصوصية</a>
      <span class="dot" aria-hidden="true">•</span>
      <a href="/about#terms">الشروط</a>
    </div>
  </div>
  <style>
/* عمودان على الشاشات الصغيرة حتى يظهر "الدعم" و"تواصل" جنباً إلى جنب */
@media (max-width: 640px){
  .farm-footer .footer-links{
    display: grid;
    grid-template-columns: repeat(2, minmax(0,1fr));
    gap: 12px 24px;
  }
}
/* ===== Footer · mobile-first refresh ===== */
.farm-footer{
  --bg:#0f172a; --fg:#e2e8f0; --muted:#94a3b8; --line:#1e293b; --card:#0b1220;
  background:var(--bg); color:var(--fg); padding:24px 0 18px; font-size:15px;
}
.farm-footer a{ color:var(--fg); text-decoration:none; }
.farm-footer a:hover{ color:#fff; text-decoration:underline; }

.farm-footer .footer-top{ 
  max-width:1100px; margin-inline:auto; padding-inline:16px;
  display:grid; gap:18px; grid-template-columns: 1fr;
}
.farm-footer .footer-brand{
  display:flex; gap:12px; align-items:flex-start; background:var(--card);
  padding:12px; border:1px solid var(--line); border-radius:14px;
}
.farm-footer .logo-mark{ width:40px; height:40px; border-radius:10px;
  display:grid; place-items:center; background:#0ea5e9; color:#00131f; font-size:20px; font-weight:900;
}
.farm-footer .brand-name{ font-weight:900; letter-spacing:.2px; }
.farm-footer .brand-desc{ margin:4px 0 0; color:var(--muted); line-height:1.5; }

/* روابط الفوتر */
.farm-footer .footer-links{ 
  display:grid; gap:14px 24px; 
  grid-template-columns: repeat(2, minmax(0,1fr)); /* عمودان على الهاتف */
}
.farm-footer .footer-links > div{
  background:transparent; padding:0;
}
.farm-footer .footer-title{
  font-size:15px; margin:0 0 6px; color:#cbd5e1; font-weight:800; letter-spacing:.2px;
}
.farm-footer ul{ list-style:none; margin:0; padding:0; display:grid; gap:6px; }
.farm-footer ul li a{ color:var(--muted); padding:4px 0; display:inline-block; }
.farm-footer ul li a:hover{ color:#fff; }

/* تواصل */
.farm-footer .contact li a[dir="ltr"]{ font-family: ui-monospace, monospace; }

/* سوشيال */
.farm-footer .footer-bottom{
  max-width:1100px; margin:18px auto 0; padding:14px 16px 0; border-top:1px solid var(--line);
  display:grid; gap:10px; text-align:center;
}
.farm-footer .social{ display:flex; justify-content:center; gap:10px; flex-wrap:wrap; }
.farm-footer .s-link{
  width:38px; height:38px; border-radius:999px; display:grid; place-items:center;
  background:#0b1220; border:1px solid var(--line); color:#cbd5e1;
  transition:transform .18s ease, background .18s ease, color .18s ease;
}
.farm-footer .s-link:hover{ transform:translateY(-2px); background:#111827; color:#fff; }

.farm-footer .copyright{ color:#94a3b8; margin:2px 0 0; font-size:13px; }
.farm-footer .mini-links{ display:flex; justify-content:center; gap:8px; color:#94a3b8; font-size:13px; }
.farm-footer .mini-links a{ color:#cbd5e1; }
.farm-footer .dot{ opacity:.5 }

/* ——— شاشة أكبر: 3 أعمدة للروابط واصطفاف علوي مرتب ——— */
@media (min-width:768px){
  .farm-footer .footer-top{
    grid-template-columns: 1.2fr 2fr;
  }
  .farm-footer .footer-links{
    grid-template-columns: repeat(3, minmax(0,1fr));
  }
  .farm-footer .footer-bottom{ grid-template-columns: 1fr auto 1fr; align-items:center; text-align:initial; }
  .farm-footer .social{ justify-content:flex-start; }
  .farm-footer .mini-links{ justify-content:flex-end; }
}

/* ——— أكورديون على الهاتف: عناوين قابلة للطي ——— */
@media (max-width:640px){
  .farm-footer .footer-links .collapsible{ border:1px solid var(--line); border-radius:12px; background:var(--card); }
  .farm-footer .footer-links .c-head{
    display:flex; align-items:center; justify-content:space-between; gap:8px;
    padding:10px 12px; cursor:pointer; user-select:none;
  }
  .farm-footer .footer-links .c-title{ margin:0; color:#cbd5e1; font-weight:900; }
  .farm-footer .footer-links .c-ico{ width:18px; height:18px; display:grid; place-items:center; color:#94a3b8; transition:transform .18s ease; }
  .farm-footer .footer-links .c-body{ overflow:hidden; max-height:0; transition:max-height .22s ease; padding:0 12px; }
  .farm-footer .footer-links .c-open .c-ico{ transform:rotate(180deg); }
  .farm-footer .footer-links .c-open .c-body{ max-height:320px; padding-bottom:10px; }
}
/* ==== Footer quick-fix: keep site colors + make it shorter ==== */

/* 1) ارجاع الألوان لألوان الموقع (لا تفرض خلفية داكنة) */
.farm-footer{
  background: transparent !important;  /* خذ لون خلفية الصفحة */
  color: inherit !important;            /* خذ لون النص الافتراضي */
  padding: 16px 0 10px !important;      /* تقليل الارتفاع العام */
  border-top: 1px solid #e5e7eb;        /* خط علوي خفيف فقط */
}
.farm-footer a{ color: inherit !important; }
.farm-footer a:hover{ color: inherit !important; text-decoration: underline; }

/* لا تعمل بطاقة داكنة لبلوك الشعار */
.farm-footer .footer-brand{
  background: transparent !important;
  border: 0 !important;
  padding: 8px 0 !important;
}
.farm-footer .logo-mark{
  background: transparent !important;   /* أزل لون المربّع */
  color: inherit !important;
}

/* 2) تقصير النص والوصف */
.farm-footer .brand-desc{
  margin: 2px 0 0 !important;
  color: inherit !important;
  display: -webkit-box;                 /* سطرين فقط */
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

/* 3) روابط الفوتر: أعمدة أقل ومسافات أصغر */
.farm-footer .footer-top{ gap: 10px !important; padding-inline: 12px !important; }
.farm-footer .footer-links{ gap: 8px 16px !important; }
.farm-footer .footer-title{ margin: 0 0 4px !important; font-size: 14px !important; }
.farm-footer ul{ gap: 4px !important; }
.farm-footer ul li a{ padding: 2px 0 !important; }

/* 4) القسم السفلي أصغر */
.farm-footer .footer-bottom{
  padding: 8px 12px 0 !important;
  gap: 6px !important;
}
.farm-footer .s-link{ width: 34px !important; height: 34px !important; }
.farm-footer .copyright, .farm-footer .mini-links{ font-size: 12px !important; }

/* 5) موبايل: عمودان فقط وروابط قابلة للطي افتراضياً لتقصير الطول */
@media (max-width:640px){
  .farm-footer .footer-links{
    grid-template-columns: repeat(2, minmax(0,1fr)) !important;
  }
  /* ابدأ مطوي */
  .farm-footer .footer-links .c-body{ max-height: 0 !important; padding-bottom: 0 !important; }
  .farm-footer .footer-links .collapsible:not(.c-open) .c-ico{ transform: rotate(0) !important; }
}
.farm-footer{ background: #f8fafc !important; }

</style>

</footer>
